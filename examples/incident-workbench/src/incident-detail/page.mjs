import {h,Shell} from '../shared/components.mjs';
export function DetailPage(){return h(Shell,{detail:true},
 h('header',{className:'intro'},h('h1',null,'INC-1042'),h('p',{className:'muted'},'Checkout requests timing out')),
 h('div',{'data-loaded':true},h('section',{className:'panel priority'},h('span',{className:'eyebrow danger'},'P1 · Investigating'),h('h2',null,'Checkout completion is at risk'),h('p',null,'Requests are waiting on an upstream dependency. Investigation is active; no mitigation has been confirmed.'),h('dl',{className:'facts'},...[['Service','Checkout API'],['Owner','Response team A'],['Opened','09:02 UTC'],['Next update','09:30 UTC']].map(([label,value])=>h('div',{className:'fact',key:label},h('dt',null,label),h('dd',null,value)))),h('a',{className:'button',href:'#response'},'Review response checklist')),
 h('div',{className:'context-grid'},h('section',{className:'panel'},h('h2',null,'Latest observations'),h('ol',{className:'timeline'},h('li',null,h('time',null,'09:18 UTC'),h('p',null,'Upstream request latency remains elevated.'),h('span',{className:'metadata'},'Response team A · Investigation')),h('li',null,h('time',null,'09:10 UTC'),h('p',null,'Affected requests isolated to checkout.'),h('span',{className:'metadata'},'Response team A · Scope check')),h('li',null,h('time',null,'09:02 UTC'),h('p',null,'Incident opened from synthetic fixture data.')))),h('section',{className:'panel',id:'response',tabIndex:-1},h('h2',null,'Response checklist'),h('p',{className:'muted'},'Read-only guidance. No production actions are performed.'),h('details',null,h('summary',null,'Review next steps'),h('ol',{className:'checklist'},h('li',null,'Confirm the impacted checkout path.'),h('li',null,'Inspect upstream dependency health.'),h('li',null,'Record the next update before handoff.')))))));}
