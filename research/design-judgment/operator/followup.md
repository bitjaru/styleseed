# Held-out fresh-session task — operator only

After initial outputs are frozen, use a new session on each arm's own result. Ask for an owner
filter that composes with the existing search/status controls, preserves detail/back navigation,
and clears selection when changed. Do not restate identity tokens or reveal another arm's output.
Evaluate preserved choices and valid adaptation, not screenshot identity. This file must not be
included in the initial agent workspace or initial prompt.
